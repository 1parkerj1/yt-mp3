## yt-mp3

- add yt-mp3 folder to path to run it from terminal

- usage:
 - `yt-mp3 -f "C:\Path\To\Folder"` (set default save folder)
 - `yt-mp3 "https://youtube.com/..."` (download audio from youtube video)
 
 - if the default folder isn't set it will automatically download to ~/Music/yt-mp3
